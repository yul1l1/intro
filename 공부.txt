js와 css를 연결해 주면서 사용자에게 보여주는게 html

F12로 elements 는 html css를 확인할 수 있고
consol로 js 확인 가능

consol은 숫자를 사용할 수 있음  

문자는 " " 를 사용해야 인식을 함

const 와 let의 차이점

console.log('');  콘솔에대한  명령어

변하지않는 고정값 사용 const a = ;
변하는 값 사용 let a =;

var 쓰지말기





단어------------------------------------------------------

integer 정수
float 소수점
string 문자로 이루워져있다
elements 요소
variables 변수
constant 상수, 변하지 않는 값